import React, { CSSProperties } from 'react';
export interface SwitchProps {
    style?: CSSProperties;
    className?: string;
}
export declare const Switch: ({ style, className }: SwitchProps) => React.JSX.Element;
